{
  "name": "askkaya-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AskKaya - Full-stack client support platform",
  "workspaces": [
    "firebase/functions",
    "web"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
